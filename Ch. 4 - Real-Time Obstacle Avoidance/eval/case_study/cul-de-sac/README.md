# Analysis workflow

The analysis assumes that there is a repository of raw data in some directory and outputs all interim files to the repository during cleaning.  

First we use `python robotracker.py path/to/run/video/`
